# Generated by aifn -- do not edit manually.
# Package: explain-stream v1.0.0

from typing import Any, Optional
from dataclasses import dataclass


@dataclass
class ExplainStreamInput:
    topic: str  # The concept, code snippet, or term to explain.
    audience: Optional[str] = None  # Target audience level (e.g. 'beginner', 'intermediate', 'expert', 'non-technical'). Default: 'intermediate'.
    context: Optional[str] = None  # Optional surrounding context (e.g. the file or system the code belongs to, or the domain the term is used in).
    depth: Optional[str] = None  # Explanation depth: 'brief' (2-3 sentences), 'standard' (1-2 paragraphs), 'detailed' (full breakdown with examples). Default: 'standard'.
    language: Optional[str] = None  # Output language. If omitted, matches the language of the topic.

