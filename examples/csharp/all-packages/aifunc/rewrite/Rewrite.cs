// Generated by aifn — do not edit manually.
// Package: rewrite v1.0.0

namespace Aifunc.Rewrite;

using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Aifunc;
using Aifunc.Engine.Csharp.V0_1_0;

/// <summary>Rewrite text according to a specified style or instruction while preserving the original meaning.</summary>
public static class Rewrite
{
	private static readonly object? MOCK_DATA = RewriteAifuncMock.MOCK_DATA;

	public static async Task<RewriteTypes.RewriteOutput> RewriteAsync(AIFuncConfig? config, RewriteTypes.RewriteInput input)
	{
		if (config is null)
			config = new AIFuncConfig { Mock = true, MockData = MOCK_DATA };
		else if (config.Mock && config.MockData is null)
			config = config.WithMockData(MOCK_DATA);


		var artifact = Artifact.FromMap(RewriteAifunc.ARTIFACT_DATA);
		var inputMap = InputToMap(input);
		var result = await Runtime.ExecuteAsync(artifact, inputMap, config).ConfigureAwait(false);
		return MapToOutput(result);
	}

	private static Dictionary<string, object?> InputToMap(RewriteTypes.RewriteInput input)
	{
		var m = new Dictionary<string, object?>();
		if (input.Instructions is not null) m["instructions"] = input.Instructions;
		m["style"] = input.Style;
		m["text"] = input.Text;
		return m;
	}

	private static RewriteTypes.RewriteOutput MapToOutput(Dictionary<string, object?> m)
	{
		string rewritten;
		if (m.TryGetValue("rewritten", out var _v0))
			rewritten = _v0 is string _s0 ? _s0 : (_v0?.ToString() ?? "");
		else
			rewritten = "";
		return new RewriteTypes.RewriteOutput(rewritten);
	}
}
