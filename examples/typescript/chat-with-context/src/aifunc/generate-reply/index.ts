// Generated by aifn -- do not edit manually.
// Package: generate-reply v1.0.0

import { execute } from '../_engine/typescript/v0.2.0/index';
import type { AIFuncArtifact, AIFuncConfig } from '../_engine/typescript/v0.2.0/index';
import artifact from './generate-reply.aifunc';
import mockData from './generate-reply.mock';
import type { GenerateReplyInput, GenerateReplyOutput } from './generate-reply.types';

export type { AIFuncConfig, GenerateReplyInput, GenerateReplyOutput };

/** Generate a contextually appropriate reply to a message or comment. */
export async function generateReply(config: AIFuncConfig = { mock: true }, input: GenerateReplyInput): Promise<GenerateReplyOutput> {
  const _projectDefaults = {};

  const runtimeConfig: AIFuncConfig = config.mock && !config.mockData
    ? { ...config, mockData }
    : config;

  return execute<GenerateReplyOutput>(
    artifact as AIFuncArtifact,
    input as unknown as Record<string, unknown>,
    runtimeConfig,
    _projectDefaults,
  );
}
