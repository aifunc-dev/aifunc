// Generated by aifn -- do not edit manually.
// Package: extract-keywords v1.0.0

import { execute } from '../_engine/typescript/v0.1.0/index';
import type { AIFuncArtifact, AIFuncConfig } from '../_engine/typescript/v0.1.0/index';
import artifact from './extract-keywords.aifunc';
import mockData from './extract-keywords.mock';
import type { ExtractKeywordsInput, ExtractKeywordsOutput } from './extract-keywords.types';

export type { AIFuncConfig, ExtractKeywordsInput, ExtractKeywordsOutput };

/** Extract keywords and key phrases from text, ranked by relevance. */
export async function extractKeywords(config: AIFuncConfig = { mock: true }, input: ExtractKeywordsInput): Promise<ExtractKeywordsOutput> {
  const runtimeConfig: AIFuncConfig = config.mock && !config.mockData
    ? { ...config, mockData }
    : config;

  return execute<ExtractKeywordsOutput>(
    artifact as AIFuncArtifact,
    input as unknown as Record<string, unknown>,
    runtimeConfig,
  );
}
