// Generated by aifn -- do not edit manually.
// Package: generate-email v1.0.0

import { execute } from '../_engine/typescript/v0.1.0/index';
import type { AIFuncArtifact, AIFuncConfig } from '../_engine/typescript/v0.1.0/index';
import artifact from './generate-email.aifunc';
import mockData from './generate-email.mock';
import type { GenerateEmailInput, GenerateEmailOutput } from './generate-email.types';

export type { AIFuncConfig, GenerateEmailInput, GenerateEmailOutput };

/** Generate a complete email from a brief description of intent and context. */
export async function generateEmail(config: AIFuncConfig = { mock: true }, input: GenerateEmailInput): Promise<GenerateEmailOutput> {
const _projectDefaults = {};

  const runtimeConfig: AIFuncConfig = config.mock && !config.mockData
    ? { ...config, mockData }
    : config;

  return execute<GenerateEmailOutput>(
    artifact as AIFuncArtifact,
    input as unknown as Record<string, unknown>,
    runtimeConfig,
    _projectDefaults,
  );
}
