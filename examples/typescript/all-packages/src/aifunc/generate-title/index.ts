// Generated by aifn -- do not edit manually.
// Package: generate-title v1.0.0

import { execute } from '../_engine/typescript/v0.1.0/index';
import type { AIFuncArtifact, AIFuncConfig } from '../_engine/typescript/v0.1.0/index';
import artifact from './generate-title.aifunc';
import mockData from './generate-title.mock';
import type { GenerateTitleInput, GenerateTitleOutput } from './generate-title.types';

export type { AIFuncConfig, GenerateTitleInput, GenerateTitleOutput };

/** Generate title or headline candidates for a piece of content. */
export async function generateTitle(config: AIFuncConfig = { mock: true }, input: GenerateTitleInput): Promise<GenerateTitleOutput> {
  const runtimeConfig: AIFuncConfig = config.mock && !config.mockData
    ? { ...config, mockData }
    : config;

  return execute<GenerateTitleOutput>(
    artifact as AIFuncArtifact,
    input as unknown as Record<string, unknown>,
    runtimeConfig,
  );
}
