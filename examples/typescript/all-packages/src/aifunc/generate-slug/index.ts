// Generated by aifn -- do not edit manually.
// Package: generate-slug v1.0.0

import { execute } from '../_engine/typescript/v0.2.0/index';
import type { AIFuncArtifact, AIFuncConfig } from '../_engine/typescript/v0.2.0/index';
import artifact from './generate-slug.aifunc';
import mockData from './generate-slug.mock';
import type { GenerateSlugInput, GenerateSlugOutput } from './generate-slug.types';

export type { AIFuncConfig, GenerateSlugInput, GenerateSlugOutput };

/** Generate an SEO-friendly URL slug, meta description, and tag suggestions from a title. */
export async function generateSlug(config: AIFuncConfig = { mock: true }, input: GenerateSlugInput): Promise<GenerateSlugOutput> {
  const _projectDefaults = {};

  const runtimeConfig: AIFuncConfig = config.mock && !config.mockData
    ? { ...config, mockData }
    : config;

  return execute<GenerateSlugOutput>(
    artifact as AIFuncArtifact,
    input as unknown as Record<string, unknown>,
    runtimeConfig,
    _projectDefaults,
  );
}
