// Generated by aifn — do not edit manually.
// Package: rewrite v1.0.0

package aifunc.rewrite;

import aifunc.AIFuncConfig;
import aifunc._engine.java.v0_1_0.Runtime;
import aifunc._engine.java.v0_1_0.Artifact;
import aifunc._engine.java.v0_1_0.Types.AIFuncArtifact;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.concurrent.CompletableFuture;

/** Rewrite text according to a specified style or instruction while preserving the original meaning. */
/** Generated by aifn — do not edit manually. */
public final class Rewrite {

	private Rewrite() {}

	private static final Object MOCK_DATA = RewriteAifuncMock.MOCK_DATA;

	public static CompletableFuture<RewriteTypes.RewriteOutput> rewrite(AIFuncConfig config, RewriteTypes.RewriteInput input) {
		if (config == null) {
			config = AIFuncConfig.builder().mock(true).mockData(MOCK_DATA).build();
		} else if (config.isMock() && config.getMockData() == null) {
			config = config.toBuilder().mockData(MOCK_DATA).build();
		}


		AIFuncArtifact artifact = Artifact.fromMap(RewriteAifunc.ARTIFACT_DATA);
		Map<String, Object> inputMap = inputToMap(input);
		return Runtime.executeAsync(artifact, inputMap, config)
			.thenApply(Rewrite::mapToOutput);
	}

	private static Map<String, Object> inputToMap(RewriteTypes.RewriteInput input) {
		Map<String, Object> m = new LinkedHashMap<>();
		if (input.getInstructions() != null) m.put("instructions", input.getInstructions());
		m.put("style", input.getStyle());
		m.put("text", input.getText());
		return m;
	}

	private static RewriteTypes.RewriteOutput mapToOutput(Map<String, Object> m) {
		String rewritten = m.containsKey("rewritten") ? (m.get("rewritten") instanceof String) ? (String) m.get("rewritten") : String.valueOf(m.get("rewritten")) : null;
		return new RewriteTypes.RewriteOutput(rewritten);
	}
}
