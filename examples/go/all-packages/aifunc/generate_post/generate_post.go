// Generated by aifn -- do not edit manually.
// Package: generate-post v1.0.0

package generate_post

import (
	"context"
	"fmt"

	engine "all-packages/aifunc/_engine/go/v0.1.0"
)

// AIFuncConfig controls the runtime mode and model connection.
type AIFuncConfig = engine.AIFuncConfig

// mockData contains the built-in offline test cases.
var mockData any = generate_postMockData

// GeneratePost Generate a social media post or short article from a topic or brief.
func GeneratePost(ctx context.Context, config *AIFuncConfig, input GeneratePostInput) (GeneratePostOutput, error) {
	if config == nil {
		config = &AIFuncConfig{Mock: true, MockData: mockData}
	}
	if config.Mock && config.MockData == nil {
		copy := *config
		copy.MockData = mockData
		config = &copy
	}

	artifact, err := engine.ArtifactFromMap(artifactData)
	if err != nil {
		return GeneratePostOutput{}, fmt.Errorf("load artifact: %w", err)
	}

	inputMap := inputToMap(input)

	result, err := engine.Execute(ctx, artifact, inputMap, *config)
	if err != nil {
		return GeneratePostOutput{}, err
	}

	return mapToOutput(result), nil
}

func inputToMap(input GeneratePostInput) map[string]any {
	m := map[string]any{}
	if input.IncludeHashtags != nil { m["includeHashtags"] = *input.IncludeHashtags }
	if input.MaxLength != nil { m["maxLength"] = *input.MaxLength }
	if input.Platform != nil { m["platform"] = *input.Platform }
	if input.Tone != nil { m["tone"] = *input.Tone }
	m["topic"] = input.Topic
	return m
}

func mapToOutput(m map[string]any) GeneratePostOutput {
	var out GeneratePostOutput
	if v, ok := m["charCount"]; ok { out.CharCount = coerceInt(v) }
	if v, ok := m["hashtags"]; ok { out.Hashtags = coerceSliceString(v) }
	if v, ok := m["post"]; ok { out.Post = coerceString(v) }
	return out
}

func coerceInt(v any) int {
	switch n := v.(type) {
	case float64:
		return int(n)
	case int:
		return n
	case int64:
		return int(n)
	}
	return 0
}

func coerceSliceString(v any) []string {
	if s, ok := v.([]string); ok { return s }
	if s, ok := v.([]any); ok {
		out := make([]string, 0, len(s))
		for _, item := range s {
			if str, ok := item.(string); ok {
				out = append(out, str)
			} else {
				out = append(out, fmt.Sprintf("%v", item))
			}
		}
		return out
	}
	return nil
}

func coerceString(v any) string {
	if s, ok := v.(string); ok { return s }
	return fmt.Sprintf("%v", v)
}

